# Docker Build Scripts

This directory contains scripts for building Docker images for the Puppy Robot project.

## Files

- `build_image.sh`: Script to build the Docker image for the project

## Usage

To build a Docker image, run:

```bash
cd /path/to/robodog
./docker/build_scripts/build_image.sh
``` 